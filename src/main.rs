mod channels;
mod logging;
mod loops;
mod mutexes;
mod random_numbers;
mod tests;
mod threads;
mod variables;

fn main() {
    // random_numbers::guess_the_number();
    // loops::loops();
    // logging::main();
    // threads::threads();
    // channels::channels();
    // channels::multiple_vals();
    mutexes::mutexes();
}
